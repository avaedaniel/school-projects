<% content_for :title, "Editing participant" %>

<h1>Editing participant</h1>

<%= render "form", participant: @participant %>

<br>

<div>
  <%= link_to "Show this participant", trip_participant_path(@trip, @participant) %> |
  <%= link_to "Back to participants", trip_participants_path(@trip) %>
</div>
